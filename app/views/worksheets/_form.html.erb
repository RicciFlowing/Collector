<%= form_for(@worksheet) do |f| %>
  <% if @worksheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@worksheet.errors.count, "error") %> prohibited this worksheet from being saved:</h2>

      <ul>
      <% @worksheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :klass %><br>
    <%= f.number_field :klass, class:'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :subj %><br>
    <%= f.text_field :subj, class:'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :file %><br>
    <%= f.file_field :file %>
  </div>
  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description, class:'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", class: 'btn btn-default' %>
  </div>
<% end %>
